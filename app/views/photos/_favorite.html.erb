<%= link_to "" , favorites_path(photo_id: photo.id) ,
                    :class => "favorite-#{photo.id} btn btn-warning",
                    :remote => true,
                    :method => :post %>