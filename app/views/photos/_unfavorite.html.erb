<%= link_to "" , favorite_path( current_user.favorites.find_by(:photo_id => photo.id) , photo_id: photo.id ) ,
                    :class => "favorite-#{photo.id} btn btn-favorite btn-default",
                    :remote => true,
                    :method => :delete %>